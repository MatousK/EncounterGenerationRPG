namespace Assets.Scripts.AI.MonsterAI
{
    /// <summary>
    /// AI for brutes. Brutes do not have any specific AI, they just hit the closest stuff, so this component does not do anything.
    /// </summary>
    class BruteAi: MonsterAiBase
    {
        protected override void Start()
        {
            base.Start();
        }

        protected override void Update()
        {
            base.Update();
        }
    }
}